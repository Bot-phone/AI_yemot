# רשימת השלוחות שאפשר להגדיר גם ע"י הטלפון
<a id="רשימת-השלוחות-שאפשר-להגדיר-גם-ע-י-הטלפון"></a>

### להלן רשימת השלוחות שאפשר להגדיר גם ע"י הטלפון

**(יש עדיין הרבה שלוחות שלא נבנה עדיין תפריט ניהול)**

ויש כמה סוגי שלוחות שלא מופיע ברשימה המושמעת בעת הבחירה

בעת הכניסה לתפריט הניהול יש לבחור בשלוחה 2 - לשינוי הגדרות שלוחה, לבחור את השלוחה המבוקשת, באם השלוחה לא קיימת הוא ישאל אם להתקין אותה ואח"כ יבקש קוד הגדרה לשלוחה יש להקיש ב3 ספרות כדלקמן, באם השלוחה מותקנת והנך מעוניין לשנות הגדרה יש ללחוץ על 9 כדי להגדיר את סוג השלוחה וכדלהלן:

**000** [תפריט](תפריט.txt#הסבר-כללי-על-מודול-תפריט)

```ini
type=menu
```

**001** [השמעת קבצים](https://xn--9dbayzv.xn--8dbbhfc1ac2gg.co.il/post/12)

```ini
type=playfile
```

**002** [חדר ועידה ושידור חי](https://xn--9dbayzv.xn--8dbbhfc1ac2gg.co.il/post/46)

```ini
type=confbridge
```

**003** [כניסה לניהול](<שלוחת ניהול המערכת.txt#כניסה-לניהול-ללא-סיסמה>)

```ini
type=admin_login
```

**004** [הקלטות מאזינים לאותה שלוחה או לשלוחה אחרת](https://xn--9dbayzv.xn--8dbbhfc1ac2gg.co.il/post/44)

```ini
type=record
```

**005** [תפריט הפניית שיחות למספרים ישראליים שונים](https://xn--9dbayzv.xn--8dbbhfc1ac2gg.co.il/post/115)

```ini
type=routing
```

**006** [הפניית שיחות לפי שעות ומספרים שונים](<ניתובי שיחה למענה אנושי - כל הסוגים (ראוטינג-ראוטינג טיים-תור-ראוטינג טלגרם>).txt#routing-ניתוב-שלוחה-למערכת-אחרת-למספר-טלפון-לכמה-מספרים-ביחד-ל1800-לIP)

```ini
type=routing_time
```

**007** [הפניית השיחה למערכת תכנים אחרת](<ניתובי שיחה למענה אנושי - כל הסוגים (ראוטינג-ראוטינג טיים-תור-ראוטינג טלגרם>).txt#routing-ניתוב-שלוחה-למערכת-אחרת-למספר-טלפון-לכמה-מספרים-ביחד-ל1800-לIP)

```ini
type=routing_yemot
```

**008** [השמעת נקודות שנצברו](<הניקוד במערכת - הסבר והגדרות.txt#שלוחת-השמעת-הנקודות-שנצברו-points_say_total>)

```ini
type=points_say_total
```

**009** [חזרה למיקום אחרון בו האזינו במערכת](https://xn--9dbayzv.xn--8dbbhfc1ac2gg.co.il/topic/95/השמעת-מיקום-האזנה-אחרונה-לפני-המעבר-להאזנה-כמו-שהיה-בivr1)

```ini
type=last_play
```

**011**   תא קולי רגיל המוגבל ל-100 הודעות

```ini
type=voicemail_email
```

**012** [הוספה אוטומטית של המאזין לרשימת התפוצה ברירת המחדל](<הוספה והסרת המספר המחייג מרשימת התפוצה.txt#הוספה-והסרת-המספר-המחייג-מרשימת-התפוצה-template_add_number-template_remove_number>)

```ini
type=template_add_number
```

**013** [הסרה אוטומטית של המאזין מרשימת התפוצה ברירת המחדל](<הוספה והסרת המספר המחייג מרשימת התפוצה.txt#הוספה-והסרת-המספר-המחייג-מרשימת-התפוצה-template_add_number-template_remove_number>)

```ini
type=template_remove_number
```

**014** [תפריט מקש כוכבית](תפריט.txt#שלוחת-כוכבית)

```ini
type=menu_star
```

**015**   הוראות הפעלה ברירת מחדל

```ini
type=message_options_menu
```

**016**   היסטוריית אזעקות צבע אדום

```ini
type=oref_alerts
```

**017**   גל השקט

```ini
type=oref_alerts_quiet_wave
```

**018**   זמני היום בהלכה

```ini
type=zmanim
```

**019**   שערי מט"ח

```ini
type=exchange_rates
```

**021**   השמעת הודעה וחזרה בכל מקש או בסיום ההודעה [יש להקליט את ההודעה בקובץ מס' M0002]

```ini
type=play_and_return
```

**022** [תא קולי ללא הגבלה ששולח העתק מההודעה לדואר אלקטרוני המוגדר במערכת](<תא קולי השולח העתק לאימייל.txt#תא-קולי-השולח-העתק-לדוא-ל-voicemail_email>)

```ini
type=voicemail_email
```

**023**   [חדר ועידה המוגבל ל-3 משתתפים שפותח לכל 3 מאזינים חדר ועידה חדשה]

```ini
type=conference_amount
```

**024** [השמעת הודעות לפי תאריך](<השמעת הודעת לפי תאריך עברי - לועזי - ספירת העומר - חנוכה.txt#השמעת-הודעת-לפי-תאריך-עברי-לועזי-ספירת-העומר-חנוכה-כולל-הגבלת-הפעמים-להשמעת-הודעה-למאזין-toplay_time>)

```ini
type=toplay_time
```

**025** [חלוקת פרקי תהילים](<חלוקת פרקי תהילים - גמרא -פרשות-דפים-עמודים.txt#חלוקת-פרקי-תהילים-גמרא-פרשות-דפים-עמודים-chapter_receive>)

```ini
type=chapter_receive
```

**026** [מערכת טריוויה לשאלות אמריקאיות או שאלות נכון ולא נכון](<שאלות טריוויה ונכון או לא נכון.txt#שאלות-טריוויה-ונכון-או-לא-נכון-trivia_questions>)

```ini
type=trivia_questions
```

**033** [השעה כעת](<שלוחת השעה כעת.txt#השעה-כעת-say_hour_and_minute>)

```ini
type=say_hour_and_minute
```

**034** [חלוקת מספרי אישורים](<חלוקת אישורים.txt#חלוקת-אישורים-approval_number>)

```ini
type=approval_number
```

**035** [קבלת הקלטות ונתונים](https://xn--9dbayzv.xn--8dbbhfc1ac2gg.co.il/topic/51/קבלת-הקלטות-ונתונים-recording_and_entering_data)

```ini
type=recording_and_entering_data
```

**036** [רשימת השמעה אישית](<רשימות השמעה אישיות.txt#מודול-רשימות-השמעה-אישיות-playlist>)

```ini
type=phone_play_list
```

**037**   פתיחת מערכת תכנים חדשה

```ini
type=create_ivr2
```

**038** [הפנייה השיחה למספר 1800 או 1801](<ניתובי שיחה למענה אנושי - כל הסוגים (ראוטינג-ראוטינג טיים-תור-ראוטינג טלגרם>).txt#routing-ניתוב-שלוחה-למערכת-אחרת-למספר-טלפון-לכמה-מספרים-ביחד-ל1800-לIP)

```ini
type=routing_1800
```
