# בדיקת הרשאת כניסה מול API  כמודול או כהטמעה בכל מודול
<a id="בדיקת-הרשאת-כניסה-מול-API-כמודול-או-כהטמעה-בכל-מודול"></a><a id="topic-8470"></a><a id="post-58432"></a>

### **מודול בדיקת הרשאת כניסה מול API**

[להטמעה בכל מודול](#בדיקת-הרשאת-כניסה-מול-שרת-API-כהטמעה-בכל-מודול)

**הסבר על המודול:** המודול בודק האם הלקוח שנכנס למערכת מורשה כניסה למערכת.

במידה שלא השיחה בכלל לא מקבלת מענה והלקוח יקבל תגובה כמו שהוא חסום ברשימת התפוצה או לא מורשה כניסה למערכת.

באם רוצים שזה יעבוד בצורה שהמערכת בכלל לא תענה לשיחה ותשמיע דחייה המודול צריך להיות **בשלוחה הראשית.**

סוג השלוחה:

```ini
type=call_api_verification
```

**חובה** לספק כתובת לבדיקה:

```ini
call_api_link=http://....
```

שימו לב. במקרה שהשרת שלכם לא מגיב - או מגיב תשובה לא תקינה - השיחה תתנתק, כנ"ל במידה שערכי החובה לא הוגדרו.

---

סוג הפניה, בחירת מחדל GET אפשר להגדיר POST:

```ini
call_api_type=POST
```

---

**ערכים שאתם מקבלים:**

`ApiDID` מספר המערכת שחוייג

`ApiPhone` טלפון שהתקשר

`ApiExtension` שלוחה

---

**תשובות**.

ניתן לענות כל תשובה, ולהגדיר לאן ללכת במידה שהיא נענתה. למשל, אם אתה רוצים לעשות `OK` למי שמורשה, יש לציין:

```ini
call_api_answer_OK=/1
```

ואז ילך לשלוחה 1 מהשלוחה הראשית.  ראה [כאן (ניתוב)](הגדרות גלובליות-הגדרות לכל השלוחות-הגדרות בכניסה לשלוחות ולמערכת-והסברים כלליים.txt#ניתוב) את האפשרויות להגדרה כולל ניתוק.

---

או אם אתם עונים `ERROR` לציין:

```ini
call_api_answer_ERROR=/7
```

ואז ילך לשלוחה 7 מהשלוחה הראשית. ראה [כאן (ניתוב)](הגדרות גלובליות-הגדרות לכל השלוחות-הגדרות בכניסה לשלוחות ולמערכת-והסברים כלליים.txt#ניתוב) את האפשרויות להגדרה כולל ניתוק.

**או,** תוכלו לענות בעצמכם כל פעם לאן ללכת. התשובה שלכם צריכה להיות למשל:

```ini
GOTO=XX
```

שבמקום ה"XX" לאן ללכת. במקרה כזה אין צורך להגדיר כלום בשלוחה ואתם כל שיחה קובעים לאן ללכת.

---

##### **קישורים לתוספים שניתנים להגדרה בכל מודול**

> ##### [רשימת כל ההגדרות שניתנות להטמעה בכל מודול](סדר פעולות בכניסה לשלוחה לפני המעבר למודול עצמו.txt#סדר-פעולות-בכניסה-לשלוחה-לפני-המעבר-למודול-עצמו)
> ##### [הגדרות זיהוי בכניסה לשלוחה](הגדרות הזיהוי בכלל המערכת.txt#כניסה-לפי-מספר-אישי)
> ##### [הסבר על הודעת ברוכים הבאים](הגדרות גלובליות-הגדרות לכל השלוחות-הגדרות בכניסה לשלוחות ולמערכת-והסברים כלליים.txt#הודעת-ברוכים-הבאים) (הודעה ראשונה בשלוחה)
> ##### [הרשאות כניסה לשלוחה](הרשאות כניסה לשלוחה.txt#להלן-ההגדרות-שקשורות-להרשאות-כניסה-לשלוחה)

---



---

<a id="בדיקת-הרשאת-כניסה-מול-שרת-API-כהטמעה-בכל-מודול"></a><a id="post-33763"></a>
## בדיקת הרשאת כניסה מול שרת  API כהטמעה בכל מודול

## **בדיקת הרשאת כניסה מול שרת  API כהטמעה בכל מודול**

ניתן להגדיר בכל כניסה לשלוחה בדיקה מול שרת API חיצוני האם הלקוח מורשה לכניסה לשלוחה.

כל הערכים הנשלחים בשלוחת API נשלחים גם בפונקציה הזו.

מגדירים:

```ini
check_api=yes
```

```ini
api_link=כתובת לבדיקה
```

האם לבדוק כל כניסה לשלוחה, או רק בניסיון הראשון (ברירת מחדל בודק רק בניסיון הראשון)

```ini
check_api_every_entry=yes
```

המערכת מבצע פעולות לפי התשובה שאתם מחזירים.

להלן התשובות האפשריות:

`OK` = ממשיך מיד לשלוחה בצורה רגילה

`NO` = יוצא מהשלוחה עם הודעה `M1102` אין לכם הרשאת גישה לתיקיה זו

`Out` = יוצא מהשלוחה ללא הודעה

בתשובות `NO` ו `Out` ניתן להגדיר לאיזה שלוחה לעבור (ברירת מחדל, חוזר שלוחה אחורה)  | ראה [כאן (ניתוב)](הגדרות גלובליות-הגדרות לכל השלוחות-הגדרות בכניסה לשלוחות ולמערכת-והסברים כלליים.txt#ניתוב)  את כל אפשרויות ההגדרה כולל ניתוק.

```ini
api_goto=/1
```

כל תשובה אחרת או שלא מתקבלת תשובה כלל, יוצא מהשלוחה ללא הודעה.

ניתן להגדיר גם במקרה כזה, לאיזה שלוחה לעבור (ברירת מחדל, חוזר שלוחה אחורה)  | ראה [כאן (ניתוב)](הגדרות גלובליות-הגדרות לכל השלוחות-הגדרות בכניסה לשלוחות ולמערכת-והסברים כלליים.txt#ניתוב)  את כל אפשרויות ההגדרה כולל ניתוק.

```ini
api_error_goto=/2
```
