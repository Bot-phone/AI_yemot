# כל סוגי השלוחות הקיימות
<a id="כל-סוגי-השלוחות-הקיימות"></a>

רשימת כל סוגי השלוחות (100) הקיימים היום

- type=menu — תפריט ([קובץ](תפריט.txt#menu-שלוחת-תפריט))

```ini
type=menu
```

- type=playfile — השמעת קבצים ([קובץ](<השמעת קבצים.txt#מודול-השמעת-קבצים-playfile-שרשור-חדש>))

```ini
type=playfile
```

- type=confbridge — חדר ועידה או שידור חי ([קובץ](https://f2.freeivr.co.il/topic/27/confbridge-חדר-וועידה-שידור-חי))

```ini
type=confbridge
```

- type=admin_login — כניסה לניהול ([קובץ](<שלוחת ניהול המערכת.txt#שלוחת-ניהול-המערכת-admin_login>))

```ini
type=admin_login
```

- type=record — הקלטות ([קובץ](https://f2.freeivr.co.il/topic/25/הקלטות-מתוך-המערכת-record))

```ini
type=record
```

- type=routing — ניתוב השיחה למספרי טלפון ישראליים ([קובץ](ניתוביה.txt#מודול-ראוטינג))

```ini
type=routing
```

- type=routing_time — ניתוב שיחה לפי שעות ([קובץ](ניתוביה.txt#ראוטינג-טיים-ניתוב-שיחה-לפי-זמנים))

```ini
type=routing_time
```

- type=routing_yemot — הפניה למערכת תכנים אחרת בימות המשיח ([קובץ](ניתוביה.txt#ראוטינג-טלגרם-העברת-שיחה-למשתמש-טלגרם))

```ini
type=routing_yemot
```

- type=points_say_total — השמעת הניקוד שנצבר ([קובץ](<הניקוד במערכת - הסבר והגדרות.txt#שלוחת-השמעת-הנקודות-שנצברו-points_say_total>))

```ini
type=points_say_total
```

- type=last_play — חזרה להשמעה אחרונה ([קובץ](<האזנה אחרונה.txt#האזנה-אחרונה-last_play>))

```ini
type=last_play
```

- type=voicemail_email — תא קולי המוגבל למאה הודעות (לא פעיל כרגע) ([קובץ](תפריט.txt#שלוחת-כוכבית))

```ini
type=voicemail_email
```

- type=template_add_number — הוספת מספר לרשימת תפוצה ([קובץ](<הוספה והסרת המספר המחייג מרשימת התפוצה.txt#הוספה-והסרת-המספר-המחייג-מרשימת-התפוצה-template_add_number-template_remove_number>))

```ini
type=template_add_number
```

- type=template_remove_number — הסרת מספר מרשימת תפוצה ([קובץ](<הוספה והסרת המספר המחייג מרשימת התפוצה.txt#הוספה-והסרת-המספר-המחייג-מרשימת-התפוצה-template_add_number-template_remove_number>))

```ini
type=template_remove_number
```

- type=menu_star — תפריט מקש כוכבית ([קובץ](תפריט.txt#השלוחות-שבשלוחת-כוכבית-ברירת-מחדל))

```ini
type=menu_star
```

- type=message_options_menu — הוראות הפעלה

```ini
type=message_options_menu
```

- type=oref_alerts — התראות צבע אדום

```ini
type=oref_alerts
```

- type=oref_alerts_quiet_wave — הגל השקט לאזעקות

```ini
type=oref_alerts_quiet_wave
```

- type=zmanim — מערכת זמני היום ([קובץ](<זמני היום והמרת תאריך עברי לועזי והפוך - זמני שבת.txt#zmanim-זמני-היום-והמרת-תאריך-עברי-לועזי-והפוך>))

```ini
type=zmanim
```

- type=exchange_rates — שערי מטח

```ini
type=exchange_rates
```

- type=play_and_return — השמעת הודעה אחת בלבד ([קובץ](https://f2.freeivr.co.il/topic/5/הגדרות-כלליות-לניהול-מערכת-טלפונית/34))

```ini
type=play_and_return
```

- type=voicemail_email — תא קולי השולח העתק לדואר האלקטרוני ([קובץ](<תא קולי השולח העתק לאימייל.txt#תא-קולי-השולח-העתק-לדוא-ל-voicemail_email>))

```ini
type=voicemail_email
```

- type=conference_amount — חדר וועידה המוגבל לכמות משתמשים

```ini
type=conference_amount
```

- type=toplay_time — השמעת הודעת לפי תאריך עברי, לועזי, ספירת העומר, חנוכה, כולל הגבלת הפעמים להשמעת הודעה למאזין ([קובץ](<השמעת הודעת לפי תאריך עברי - לועזי - ספירת העומר - חנוכה.txt#השמעת-הודעת-לפי-תאריך-עברי-לועזי-ספירת-העומר-חנוכה-כולל-הגבלת-הפעמים-להשמעת-הודעה-למאזין-toplay_time>))

```ini
type=toplay_time
```

- type=chapter_receive — חלוקת פרקי תהילים, גמרא, פרשות, דפים ועמודים ([קובץ](<חלוקת פרקי תהילים - גמרא -פרשות-דפים-עמודים.txt#חלוקת-פרקי-תהילים-גמרא-פרשות-דפים-עמודים-chapter_receive>))

```ini
type=chapter_receive
```

- type=trivia_questions — שאלות טרוויה ונכון או לא נכון ([קובץ](<שאלות טריוויה ונכון או לא נכון.txt#שאלות-טריוויה-ונכון-או-לא-נכון-trivia_questions>))

```ini
type=trivia_questions
```

- type=open_wap_reseller_customer — פתיחת וצאפפון ובלוג פון ([קובץ](https://f2.freeivr.co.il/topic/5/#:~:text=ווצאפון%20מהמערכת%20שלכם))

```ini
type=open_wap_reseller_customer
```

- type=say_hour_and_minute — השמעת השעה כעת

```ini
type=say_hour_and_minute
```

- type=approval_number — חלוקת מספרי אישורים

```ini
type=approval_number
```

- type=recording_and_entering_data — קבלת נתונים

```ini
type=recording_and_entering_data
```

- type=phone_play_list — רשימות השמעה אישיות

```ini
type=phone_play_list
```

- type=create_ivr2 — פתיחת מערכת תכנים חדשה

```ini
type=create_ivr2
```

- type=routing_1800 — הפניית השיחה למספרי 1800 ו 1801

```ini
type=routing_1800
```

- type=menu_voice — תפריט עם זיהוי דיבור קולי

```ini
type=menu_voice
```

- type=sending_sms — שליחת sms

```ini
type=sending_sms
```

- type=go_to_folder_time — שלוחת העברה לתיקיות אחרות לפי שעות או ימים

```ini
type=go_to_folder_time
```

- type=stt_dir_all_file — שלוחת המרת קבצי קול לטקסט

```ini
type=stt_dir_all_file
```

- type=points_save — שלוחת הוספת ניקוד

```ini
type=points_save
```

- type=hangup — שלוחת ניתוק השיחה

```ini
type=hangup
```

- type=start_select_file — שלוחה המאפשרת למאזין לבחור האם בשלוחת השמעת קבצים יהיה לו תפריט בחירת קובץ או לא

```ini
type=start_select_file
```

- type=playdir_time — שידור חי מדומה, מרובה קבצים, השמעת כל הקבצים בשלוחה בהתאם לזמן המוגדר

```ini
type=playdir_time
```

- type=private_did_customer — קבלת או שליחת מספר אישי

```ini
type=private_did_customer
```

- type=tzintuk — הפעלת צינתוק, ושלוחה להרשמה או הוספה מרשימת הצינתוקים

```ini
type=tzintuk
```

- type=go_to_folder_from_list_all_information — פילטר לפי רשימות, העברה לשלוחה אחרת לפי הגדרות ספציפיות ללקוח לפי קובץ

```ini
type=go_to_folder_from_list_all_information
```

- type=nedarim_plus — סליקת אשראי דרך נדרים פלוס

```ini
type=nedarim_plus
```

- type=pelecard — סליקת אשראי דרך פלאקארד

```ini
type=pelecard
```

- type=cardcom — סליקת אשראי דרך קרדקום

```ini
type=cardcom
```

- type=sale_seats — מערכת מכירת מושבים

```ini
type=sale_seats
```

- type=chapter_receive_mishnayot_select — מערכת חלוקת משניות, כולל שמיעת המשניות משלוש רבנים, ודוחות לימוד

```ini
type=chapter_receive_mishnayot_select
```

- type=study_tracking — מעקב לימודים

```ini
type=study_tracking
```

- type=go_to_folder_count — מעבר לשלוחות שונות, כשיש פחות או יותר מכמות, כל שהיא, של מאזינים שעברו במודול הזה

```ini
type=go_to_folder_count
```

- type=go_to_folder — מעבר לשלוחה אחרת

```ini
type=go_to_folder
```

- type=music_on_hold — מוזיקה בהמתנה ושידורים חיים מערוצים שונים

```ini
type=music_on_hold
```

- type=queue — מודל תור

```ini
type=queue
```

- type=send_fax — שליחת פקס

```ini
type=send_fax
```

- type=recv_fax — קבלת פקס

```ini
type=recv_fax
```

- type=examination — מודל מבחן

```ini
type=examination
```

- type=seker — שלוחת סקר

```ini
type=seker
```

- type=fundraising — מודול מעקב גיוס תרומות

```ini
type=fundraising
```

- type=members_active — מודול כמות חברים פעילים

```ini
type=members_active
```

- type=counter_calculator — מודל חישוב מונה

```ini
type=counter_calculator
```

- type=donation_campaign — מודל התרמות, מצינג

```ini
type=donation_campaign
```

- type=points_edit — מודול הוספת או הורדת נקודות ללקוח על ידי מנהלים

```ini
type=points_edit
```

- type=yemot_dialer_campaign_list — מודול הוספה, הסרה או חסימה מתבניות רשימות התפוצה

```ini
type=yemot_dialer_campaign_list
```

- type=tehillim_select_save_and_listen — מודול בחירת ושמירת פרק תהילים קבוע ללקוח

```ini
type=tehillim_select_save_and_listen
```

- type=points_to_other_id — הוספת ניקוד למשתמש אחר

```ini
type=points_to_other_id
```

- type=api — תקשור עם מחשבים וממשקי נתונים חיצוניים

```ini
type=api
```

- type=examination_american — מבחן אמריקאי

```ini
type=examination_american
```

- type=key_play_set_record_go_to_foldar — כניסה לפי מפתח, הגבלת כניסות, השמעת הודעה, תפריט, הקלטה, ומעבר לתיקיה אחרת

```ini
type=key_play_set_record_go_to_foldar
```

- type=block_id_type — חסימת משתמש

```ini
type=block_id_type
```

- type=coupon — חלוקת קופונים

```ini
type=coupon
```

- type=credit_card — התחברות למנוע סליקה כללי

```ini
type=credit_card
```

- type=id_message — השמעת נתונים והודעות אישיות

```ini
type=id_message
```

- type=id_list_message — השמעת הודעות ללקוח ברצף

```ini
type=id_list_message
```

- type=record_system_messages — הקלטת הודעות מערכת בשלוחה

```ini
type=record_system_messages
```

- type=folder_play_random — הפעלת שלוחה רנדומלית

```ini
type=folder_play_random
```

- type=playfile_time — הפעלת קובץ בשעה מתוזמנת, שידור חי מדומה

```ini
type=playfile_time
```

- type=yemot_dialer_campaign_start — הפעלה מהירה לקמפיין

```ini
type=yemot_dialer_campaign_start
```

- type=template_filter — הפניית משתמשים לשלוחות שונות לפי רשימת התפוצה

```ini
type=template_filter
```

- type=sale_products — הזמנת מוצרים לפי מספר קטלוגי

```ini
type=sale_products
```

- type=daily_message — הודעה יומית מתחלפת ומעבר לשלוחה אחרת

```ini
type=daily_message
```

- type=go_to_folder_date — ההעברה לשלוחה אחרת, לפי תאריך עברי או לועזי או ו לפי שעה או דקה

```ini
type=go_to_folder_date
```

- type=tasks_report — דיווח משימות יומי

```ini
type=tasks_report
```

- type=checking_units — בדיקת יתרת יחידות נמוכה או פג תוקף מתקרב

```ini
type=checking_units
```

- type=time_keeper — שעון נוכחות

```ini
type=time_keeper
```

- type=routing_ip — ניתוב שיחה למחשב חיצוני

```ini
type=routing_ip
```

- type=points_and_rating — מודול נקודות ללקוח על קניה, ודירוג בעל העסק

```ini
type=points_and_rating
```

- type=limit_call_seconds — הגבלת אורך השיחה, טיימר

```ini
type=limit_call_seconds
```

- type=calculator_add_percentage — מחשבון אחוזי תוספת

```ini
type=calculator_add_percentage
```

- type=access_filter — פילטר כניסה לפי תאריך עם אפשרויות שונות ואפשרות הגבלת כמות הכניסות

```ini
type=access_filter
```

- type=number_now_is — המספר כעת הוא

```ini
type=number_now_is
```

- type=say_amount_subscribers — השמעת כמות נרשמים מתוך שלוחת קבלת נתונים

```ini
type=say_amount_subscribers
```

- type=amount_incoming_phone_numbers — ספירת כמות מספרי טלפון שונים שנכנסו לשלוחה

```ini
type=amount_incoming_phone_numbers
```

- type=nitoviya — קבלת או שליחת ניתוביה

```ini
type=nitoviya
```

- type=nitoviya_b_say_number — שמיעת מספר ניתוביה

```ini
type=nitoviya_b_say_number
```

- type=donation_campaign_some_actions — שלוחת מצינג עם הגדרות נוספות

```ini
type=donation_campaign_some_actions
```

- type=private_did — הוצאת וקבלת שיחות

```ini
type=private_did
```

- type=add_id_to_list — הוספת ערך ID של המתקשר או ערך מוגדר מראש לקובץ במערכת לבחירתכם ([קובץ](<שלוחת הוספת הסרת ושינוי ערכים וערך ID מקבצי הגדרות במערכת.txt#מודול-הוספת-ערך-ID-של-המתקשר-או-ערך-מוגדר-מראש-לקובץ-במערכת-לבחירתכם>))

```ini
type=add_id_to_list
```

- type=remove_id_from_list — הסרת ערך ID של המתקשר או ערך מוגדר מראש מקובץ במערכת לבחירתכם ([קובץ](<שלוחת הוספת הסרת ושינוי ערכים וערך ID מקבצי הגדרות במערכת.txt#מודול-הסרת-ערך-ID-של-המתקשר-או-ערך-מוגדר-מראש-מקובץ-במערכת-לבחירתכם>))

```ini
type=remove_id_from_list
```

- type=yemot_dialer — שלוחה לניהול קמפיין ([קובץ](<שלוחה לניהול קמפיין.txt#שלוחה-לניהול-קמפיין>))

```ini
type=yemot_dialer
```

- type=sending_sms_and_return — שליחת sms ללקוח בכניסה לשלוחה ומעבר לשלוחה אחרת ([קובץ](<שליחת SMS למתקשר או לאחרים.txt#מודול-שליחת-sms-ללקוח-בכניסה-לשלוחה-ומעבר-לשלוחה-אחרת>))

```ini
type=sending_sms_and_return
```

[חזור למעלה](#כל-סוגי-השלוחות-הקיימות)
